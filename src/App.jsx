import React, { useState } from 'react';\nimport OnboardingScreen from './screens/OnboardingScreen';\nimport MainDashboard from './screens/MainDashboard';\nimport TryOnStudio from './screens/TryOnStudio';\nimport ProfileScreen from './screens/ProfileScreen';\n\nconst App = () => {\n  const [currentScreen, setCurrentScreen] = useState('home'); // 'home', 'onboarding', 'dashboard', 'tryon', 'profile'\n  const [cloneGenerated, setCloneGenerated] = useState(false);\n  const [cloneImage, setCloneImage] = useState(null);\n\n  const handleStartOnboarding = () => {\n    setCurrentScreen('onboarding');\n  };\n\n  const handleCloneGenerated = (image) => {\n    setCloneImage(image);\n    setCloneGenerated(true);\n    setCurrentScreen('dashboard');\n  };\n\n  const handleNavigate = (screen) => {\n    if (cloneGenerated) {\n      setCurrentScreen(screen);\n    } else if (screen === 'onboarding') {\n      setCurrentScreen('onboarding');\n    }\n  };\n\n  return (\n    <div className=\"bg-gray-50 min-h-screen\">\n      {currentScreen === 'home' && !cloneGenerated && (\n        <div className=\"min-h-screen flex flex-col items-center justify-center px-6 bg-gradient-to-b from-blue-50 to-purple-50\">\n          <div className=\"text-center max-w-md animate-fade-in\">\n            <div className=\"mb-8\">\n              <div className=\"w-24 h-24 mx-auto bg-gradient-primary rounded-full flex items-center justify-center text-white text-4xl font-bold\">\n                AI\n              </div>\n            </div>\n            <h1 className=\"text-4xl font-bold text-gray-900 mb-3\">MyClone AI</h1>\n            <p className=\"text-gray-600 text-lg mb-2\">Universal AI Trial Room</p>\n            <p className=\"text-gray-500 text-sm mb-8\">Create your AI Clone and try on anything virtually</p>\n            \n            <button\n              onClick={handleStartOnboarding}\n              className=\"w-full gradient-primary text-white py-4 rounded-xl font-semibold text-lg hover:shadow-lg transition-all duration-300 mb-4\"\n            >\n              Create Your AI Clone\n            </button>\n            <p className=\"text-gray-400 text-xs\">Takes 15-20 minutes to generate</p>\n          </div>\n        </div>\n      )}\n\n      {currentScreen === 'onboarding' && (\n        <OnboardingScreen onCloneGenerated={handleCloneGenerated} />\n      )}\n\n      {currentScreen === 'dashboard' && cloneGenerated && (\n        <MainDashboard cloneImage={cloneImage} onNavigate={handleNavigate} />\n      )}\n\n      {currentScreen === 'tryon' && cloneGenerated && (\n        <TryOnStudio cloneImage={cloneImage} onNavigate={handleNavigate} />\n      )}\n\n      {currentScreen === 'profile' && cloneGenerated && (\n        <ProfileScreen onNavigate={handleNavigate} />\n      )}\n    </div>\n  );\n};\n\nexport default App;\n